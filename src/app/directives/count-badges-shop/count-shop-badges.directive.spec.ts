import { CountShopBadgesDirective } from './count-shop-badges.directive';

describe('CountShopBadgesDirective', () => {
  it('should create an instance', () => {
    const directive = new CountShopBadgesDirective();
    expect(directive).toBeTruthy();
  });
});
