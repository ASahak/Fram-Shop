import { SortShopProductsDirective } from './sort-shop-products.directive';

describe('SortShopProductsDirective', () => {
  it('should create an instance', () => {
    const directive = new SortShopProductsDirective();
    expect(directive).toBeTruthy();
  });
});
